import type { PromptOptions } from "../../llm/prompt.js";
import type { ChatMode } from "../../chat/mode.js";
export function featurePrompt(_opts: PromptOptions, _mode: ChatMode): string { return ""; }
export const featureExamples: Record<string, unknown> = {};
